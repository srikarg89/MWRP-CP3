First row is 3 numbers: # of agents (A), # of map rows (R), and # of map columns (C).

The next A rows have two numbers, the X and Y coordinates of that correspondin agent.

The next R rows describe the map (see figure1.txt for an example). An X indicates an occupied square and a # indicates a free square.
